export interface Category{
    idCategoryLeague?: number,
    name: String,
    isActive: boolean,
    minAge: Number,
    maxAge: Number, 
    description: String,
    duration: Number,
    sex: String
}